import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['cdn.forzasys.com', 'video-ntf.forzify.com', 'd22hh18o76pkhl.cloudfront.net', "norkring-ntf-od.telenorcdn.net"],
  },
};

export default nextConfig;
